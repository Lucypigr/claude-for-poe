{
  "name": "claude-for-poe",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Original 2.5D Web ARPG built with Three.js and Vite.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:cloudflare": "vite build --mode cloudflare",
    "preview": "vite preview",
    "preview:cloudflare": "vite preview --mode cloudflare",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.19"
  },
  "dependencies": {
    "three": "^0.186.1"
  },
  "devDependencies": {
    "vite": "^8.3.1"
  }
}
